# 📦 Migration Notice

`mupeng-protocol` 레포가 `mupengism`으로 통합되었습니다.

**무펭이즘 = 사상 + 구현체**

- 철학/윤리/원칙 → 루트 디렉토리
- 프로토콜 기술 문서 → `protocol/` 디렉토리
- SDK/설치 도구 → `installer/`, `scripts/`

기존 `mupeng-protocol` 레포는 이 레포로 리다이렉트됩니다.
